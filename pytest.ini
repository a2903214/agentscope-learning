[pytest]
# test root; Test Explorer discovers test_* and Test* here
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
# -s: no capture so test and subprocess output show in console
addopts = --tb=short -v -s
# asyncio_mode = auto  # if using pytest-asyncio
