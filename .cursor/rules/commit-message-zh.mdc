---
description: 本工程 Git 提交注解一律使用中文
alwaysApply: true
---

# Git 提交注解使用中文

## 规则

- **所有 Git 提交说明（commit message）必须使用中文书写。**
- 适用于本仓库（agentscope-learning）内任何由 AI 协助生成的 `git commit -m "..."` 或提交信息。

## 格式建议

- **标题**：简短概括，使用中文，如 `功能：xxx`、`修复：xxx`、`文档：xxx`
- **正文**（可选）：分条列举变更，使用中文

## 示例

```text
功能(70-examples)：中文提示与工具描述、超时与测试无缓冲

- main：仅中文系统/用户提示，top_pick 兼容字典与字符串，日志中文
- tools：注释与工具 docstring 全改为中文
- global_model_config：客户端超时 60s 连接 / 300s 读取
- tests：SUBPROCESS_ENV 含 PYTHONUNBUFFERED，子进程日志可输出
```

## 注意

- 使用中文可避免在 Windows 终端下因编码导致的提交信息乱码。
- 若用户明确要求使用英文提交信息，则以用户要求为准。
